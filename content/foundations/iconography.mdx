---
title: Iconography
description: Icons help teachers spot an action faster than words alone. One consistent set for the UI, a warmer hand-drawn style for brand moments.
status: proposed
sections:
  ui-icons-lucide: "The icon set for interface work — nav, controls, product UI. Use Lucide, and don't mix in another set."
  brand-icons-icon-generator: "The warmer, hand-drawn icons for brand and marketing moments. Use these for comms, not dense UI — they're slower to scan."
  usage: "How to drop an icon into code, sized and labelled — including the aria-label every icon-only button needs."
  best-practices: "The habits that keep icons clear and consistent — which set goes where, and the traps to avoid."
tools:
  - name: Icon Generator
    description: Produces on-brand brand and feature iconography. Stroke weight, corner radius, and silhouette are enforced by construction, so the output is on-standard before review.
    href: https://icon-generator-seven.vercel.app/
    repo: https://github.com/wondopamine/icon-generator
---

## UI icons — Lucide

Interface icons come from **Lucide** (the shadcn/ui companion set): consistent stroke weight, predictable naming, tree-shakeable, and a vocabulary AI agents already know. Don't mix icon sets in product UI — this is the vocabulary for system interfaces: nav, controls, product UI.

<IconSet />

## Brand icons — Icon Generator

Brand and feature iconography is generated with the internal **Icon Generator**, which maintains stroke weight, corner radius, and silhouette rules by construction.

<BrandIconSet />

Use the inked style for product marketing and communications, not dense UI — it's slower to scan than Lucide.

## Usage

```tsx
import { Calendar } from "lucide-react";

<Calendar aria-hidden size={20} strokeWidth={2} />

{/* An icon-only button carries a label */}
<button aria-label="Open calendar"><Calendar aria-hidden /></button>
```

## Best practices

<DoDont items={[
  { kind: "do", text: "Use Lucide for interface icons, and the ink set for marketing and comms." },
  { kind: "do", text: "Give every icon-only button a descriptive aria-label (A11Y-3)." },
  { kind: "do", text: "Match an icon's stroke weight to the adjacent text weight." },
  { kind: "dont", text: "Mix icon sets inside product UI." },
  { kind: "dont", text: "Stack an icon in a decorative tile above its heading (SLP-5)." },
]} />
