# Platform adoption contract fixture

The values in this directory are deterministic contract samples for AE1. They are
not settled Platform or EduPass brand facts. The fixture exists only to prove that
an explicit Platform product is graded against its own declaration and never receives
the Teachers & School compatibility profile.
