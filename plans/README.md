# Implementation Plans — TFX → DXD Design Standard

Generated by the improve skill on 2026-07-10, against commit `233f3be`. These plans
execute the decisions in `docs/brainstorms/2026-07-10-dxd-design-standard-requirements.md`
(R1–R16): evolve this repo from the TransformX team-level standard into the DXD
division-level standard — one foundation, four domain expressions (Teachers & School,
Students, Parents, Platform incl. EduPass).

**Not** the same directory as `harness/plans/` — that is the harness's own historical
improvement backlog. This directory is the DXD program plan set.

Each executor: read the plan fully before starting, honor its STOP conditions, run every
verification gate, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Executor model | Depends on | Status |
|------|-------|----------|--------|----------------|------------|--------|
| 001 | Rename TFX → DXD (plugin, harness, site identity) | P1 | L | Sonnet | — | DONE — branch `advisor/001-dxd-rename` @ `c839276` (reviewed, all gates pass; awaiting user merge) |
| 002 | Domain registry, profile schema, catalog `domains:` scope | P1 | M | Opus | 001 (naming only) | DONE — branch `advisor/002-domain-profile-schema` @ `7f6141b` (reviewed, all gates pass) |
| 003 | De-brand skills; brand resolves from profile at run time | P1 | M | Opus | 002 | DONE — branch `advisor/003-debrand-skills` @ `b8811ae` (reviewed, all gates pass; leakage audit clean) |
| 004 | Setup skill → domain-onboarding wizard | P1 | M | Opus | 002 | DONE — branch `advisor/004-onboarding-wizard` @ `e7c490e` (reviewed, all gates pass; rehearsal artifacts in executor report) |
| 005 | Website Domains section (nav + 4 domain pages) | P2 | M | Sonnet | 002 | DONE — branch `advisor/005-domains-section` @ `219bf63` (reviewed, all gates pass; thumbnails for /domains fall back to tinted box — cosmetic, noted for follow-up) |
| 006 | "Get started" onboarding page with visualisations | P2 | M–L | Opus (via the repo's own design loop) | 004, 005 | DONE — branch `advisor/006-onboarding-page` @ `7857f0f` (independent evaluator pass: REVISE → fixed → approved). This branch is the INTEGRATION branch: it contains 001–007 merged; merging it alone delivers the whole program. |
| 007 | Governance + CONTRIBUTING for domains; EduPass pilot playbook | P2 | S–M | Sonnet | 002 | DONE — branch `advisor/007-governance-pilot` @ `b27c81c` (reviewed, all gates pass) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Review-fix plan set (008–015, added 2026-07-11)

Written by the improve skill against commit `233f3be` (branch
`standards/glow-pilot-fixes`), from a 10-finding adversarial code review of
`main...233f3be` (8 finder angles, per-finding verification). Independent of
the DXD program above; plan 015 notes its merge interplay with
`advisor/006-onboarding-page`.

| Plan | Title | Priority | Effort | Executor model | Depends on | Status |
|------|-------|----------|--------|----------------|------------|--------|
| 008 | Anchor `.claude/` gitignore so `harness/.claude/` is tracked | P1 | S | Haiku | — | DONE — worktree branch `worktree-agent-a1f43595e806630a3` @ `170b459` (reviewed, approved) |
| 009 | DocPage MDX fallback: build-time signal, no dead ToC, active-voice note | P1 | M | Opus | — | DONE — worktree branch `worktree-agent-a686c03cde5cf53e7` @ `53f54a8` (reviewed, approved; fallback rehearsal evidence in executor report) |
| 010 | detect.py ignoreFiles expansion survives real repos (prune + filter) | P1 | M | Opus | — | DONE — worktree branch `worktree-agent-a9783c13800c570f8` @ `f817b9e` (reviewed, approved; self-test 35→40 cases) |
| 011 | Catalog `status: proposed` field; CNT-5/6/7 stop rendering as settled | P1 | M | Fable | — | DONE — worktree branch `worktree-agent-a82b341a9e8ad461f` @ `aa1f418` (reviewed, approved; validators re-run, 48 self-test cases) |
| 012 | CI runs the Python standards gate (validate.py + static checks) | P1 | S | Sonnet | — | DONE — worktree branch `worktree-agent-a020c04d706d918a3` @ `f1a6e47` (reviewed, approved; four gate commands rehearsed locally) |
| 013 | Design hook fires on .ts/.js like the checkers it fronts | P2 | S | Sonnet | — | DONE — worktree branch `worktree-agent-a25c9181f63fd0c71` @ `a9b019f` (reviewed, approved; hook self-test 25→27 cases) |
| 014 | content-lint strips list/blockquote markers for anchored checks | P2 | M | Sonnet | — | DONE — worktree branch `worktree-agent-a7d81f5b6255b1911` @ `c0acb6b` (reviewed, approved; blast-radius delta in executor report — 1 new true CNT-6 finding in harness/docs) |
| 015 | Stale skill names, dead docs link, drifted counts sweep | P2 | S–M | Sonnet | — | DONE — worktree branch `worktree-agent-a3e9874bddb4d144f` @ `99e38a5` (reviewed, approved; stale-name greps clean) |

Model rationale: Fable on 011 (cross-cutting schema + governance judgment);
Opus where behaviour judgment is the work (009 RSC fallback semantics, 010
scale reasoning + self-tests); Sonnet for pattern-following verifiable changes;
Haiku only on 008 (one line, fully mechanical gates). All eight are mutually
independent and touch disjoint files.

**Execution outcome (2026-07-11).** All eight executed in isolated worktrees
(note: the worktrees were cut from `5327829` = origin/main, not `233f3be`; every
plan's in-scope files were verified identical between the two, so no drift) and
reviewed: scope clean, gates re-run by the reviewer. The eight tips were
octopus-merged onto `233f3be` in a disposable worktree and the full gate suite
re-run on the merged tree (validate 48-case self-test, detect 40, hook 27,
content-lint 42, both validators OK, 21 vitest tests, `pnpm build` 196 pages,
no `[doc-page]` fallbacks). The verified merge is branch
**`advisor/008-015-review-fixes`** @ `af0ffd4`. Nothing is merged to any user
branch — landed on `standards/glow-pilot-fixes` (now `af0ffd4`, pushed
2026-07-11 with the operator's approval; CI's new Python gate runs on it).

**DXD + review-fixes integration (2026-07-11).** `advisor/006-onboarding-page`
was merged onto `af0ffd4` in a disposable worktree; two prose conflicts
(README.md, content/harness/skills.mdx) resolved in DXD's favour with plan 015's
corrected facts (eleven skills, `dxd:copy`, `/dxd:start` — the DXD rename had
mechanically inherited the stale four-skill roster). Full gates green on the
merged tree: validate 58-case self-test, detect 40, hook 27, content-lint 42,
both validators OK (60 controls, 30 docs), 22 vitest tests, 210-page build, no
`[doc-page]` fallbacks, stale-name grep clean (both `tfx:` and ghost `dxd:`
names). The verified result is branch **`advisor/dxd-plus-review-fixes`** @
`a8845a3` — merging it into `standards/glow-pilot-fixes` lands the whole DXD
program plus all review fixes in one commit. Open governance question left for the
design lead (plan 011): should enforced checks skip `status: proposed` controls
or keep flagging them as advisory? Follow-up candidates deliberately deferred:
one shared extension constant across checkers/hook/detect (7-8 copies today);
COUNT-SYNC coverage for `public/` pages; the new CNT-6 true-positive in
`harness/docs/catalog-changes/cmp-4-empty-state-clarity.md:36`.

## Dependency notes

- **001 first** so every later artifact is born DXD-named. Plans 002–007 are written
  rename-aware (`dxd-waive`, `.dxd/`, `/dxd:`); if executed before 001, those literals
  land early — harmless, but re-run 001's grep inventories afterwards.
- **002 is the keystone**: it defines the domain registry, profile schema, and
  resolution order (product DESIGN.md > domain profile > foundation default) that
  003, 004, 005, and 007 all consume. Nothing else should start before it merges.
- **003 and 004 can run in parallel** after 002 (they touch different skill
  directories; 003 explicitly skips setup/ files if 004 has landed — coordinate if
  truly concurrent).
- **005 can run in parallel with 003/004** after 002.
- **006 last**: it documents the wizard's real steps (R13 — one story, same step
  names) and links the domain pages; building it against imagined steps is a STOP
  condition in the plan.
- **007** needs only 002 to document the rules, but its pilot playbook's entry
  criteria require 001–006 merged — write anytime after 002, run the pilot after 006.

## Model assignment rationale

Requested by the operator: match model tier to difficulty and importance.

- **Opus** where judgment is the work: 002 (the data model everything consumes),
  003 (every sentence decides foundation vs brand; routing descriptions are
  behaviour), 004 (conversational UX for non-technical users), 006 (design quality
  is the deliverable; runs the repo's own design loop + evaluator).
- **Sonnet** where a strong pattern already exists and every change is
  mechanically verifiable: 001 (grep-gated rename), 005 (mirrors the existing
  content-section architecture), 007 (documents already-made decisions).
- Nothing was assigned to Haiku: even the mechanical plans carry repo-wide blast
  radius (001) or public prose held to SLP-9 (007).

## Program-level exit (from the requirements doc)

The program is done when the EduPass pilot (plan 007's playbook) exits: one real
screen shipped through the loop under EduPass branding with zero Teacher & School
leakage, the domain lead's explain-back of the Get started page was substantially
correct, and existing TFX installs survived the rename via the migration path.

## Execution outcome (2026-07-10)

All seven plans executed by dispatched agents in isolated worktrees and reviewed. **Nothing
is merged to any user branch** — merging is the operator's decision. The branch stack:

`233f3be` (session HEAD) → `advisor/001-dxd-rename` (c839276) → `advisor/002-domain-profile-schema`
(7f6141b) → three parallel branches (`advisor/003-debrand-skills` b8811ae ·
`advisor/004-onboarding-wizard` e7c490e · `advisor/005-domains-section` 219bf63 ·
`advisor/007-governance-pilot` b27c81c) → **`advisor/006-onboarding-page` (7857f0f) = the
integration branch containing everything**. To land the whole program:
`git merge advisor/006-onboarding-page` (all gates were green on that exact commit).

Residuals deliberately left, for follow-up:
- `content/sections/for-agents.mdx` + `content/sections/standards.mdx` still teach `tfx-waive`
  syntax examples (website instructional text; legacy syntax remains valid, so not wrong — just stale).
- `harness/standards/catalog.yaml` line-1 header comment still says "TFX Design Standard" (catalog
  body edits were scoped out of the rename).
- `harness/CLAUDE.md` "Where things live" table row still says "copy skill (TFX voice & tone)".
- `harness/docs/index.html` install snippet still says `install tfx@tfx`.
- `/domains` topic thumbnails fall back to a tinted box (icon pipeline has no domain glyphs yet).
- `harness/docs/UPDATING.md` historical migration sections keep `/tfx:` commands on purpose (history).
- Human coordination step: rename the GitHub repo to `dxd-design-standard` (see `harness/docs/MIGRATION-DXD.md`).
- Ratchet candidate from the evaluator: a control asserting division-neutrality of cross-domain
  surfaces (no domain's brand stated as foundation fact; no domain token as the neutral accent).

## Findings considered and rejected

- **Four top-level domain buttons in the site nav**: rejected in brainstorm — three
  stub domains as primary nav overstates parity; one "Domains" item, revisit when a
  second domain reaches `settled`.
- **New separate `dxd-design-standard` repo**: rejected — one source of truth, history
  preserved; TFX becomes a domain profile inside this repo. The GitHub repo *rename*
  remains a flagged human step (plan 001).
- **Domain override rights on foundation controls**: rejected — additive-only; an
  overridable foundation stops being one.
- **Moving `/products/*` under `/domains/teachers-school/…`**: rejected — URL breakage
  for a presentational gain; the T&S domain page links the existing product pages.
- **Non-web check adapters**: deferred, not planned — declared boundary; all near-term
  adopters are web-stack. Revisit if the pilot contradicts this.
- **Renaming historical records (harness/plans, decisions, changelog entries)**:
  rejected — renaming history falsifies it; every plan's scope excludes them.
